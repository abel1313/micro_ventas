spring:
  profiles:
    active: dev   # perfil que arranca por defecto
  datasource:
    url: jdbc:mysql://167.88.164.132:3306/dummy_pruebas
    username: trece
    password: Luvianos*13
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
server:
  port: 9094
  servlet:
    context-path: /mis-productos/

---
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:mysql://prod-server:3306/db_prod
    username: ${DB_PROD_USER}
    password: ${DB_PROD_PASS}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

---
endpoint: http://localhost:8081/mis-productos
api:
  venta-service: ${endpoint}/ventas
  venta-detalle-ventas: ${endpoint}/detalle-ventas
  productos: ${endpoint}/productos
